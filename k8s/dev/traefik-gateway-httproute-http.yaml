apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: traefik-gateway-webappdev-http
  namespace: dev
spec:
  gatewayClassName: traefik
  listeners:
  - name: web
    hostname: webappdev.riives.eu
    port: 80
    protocol: HTTP
    allowedRoutes:
      namespaces:
        from: All
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: web-app
  namespace: dev
spec:
  hostnames:
  - webappdev.riives.eu
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: traefik-gateway-webappdev-http
    sectionName: web
    namespace: dev
  rules:
  - backendRefs:
    - group: ""
      kind: Service
      name: web-app-service
      port: 80
      weight: 1
    matches:
    - path:
        type: PathPrefix
        value: /
